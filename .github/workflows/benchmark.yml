name: Multi-Platform Benchmark

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  benchmark:
    runs-on: ${{ matrix.os }}
    timeout-minutes: 30
    
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-2025, macos-latest]
        include:
          - os: ubuntu-latest
            platform: debian
          - os: windows-2025
            platform: windows
          - os: macos-latest
            platform: mac-arm
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Install dependencies (Ubuntu)
      if: matrix.os == 'ubuntu-latest'
      run: |
        sudo apt-get update
        sudo apt-get install -y hyperfine python3 clang
    
    - name: Install dependencies (macOS)
      if: matrix.os == 'macos-latest'
      run: |
        brew install hyperfine
    
    - name: Install dependencies (Windows)
      if: matrix.os == 'windows-2025'
      run: |
        winget install hyperfine --accept-source-agreements --accept-package-agreements
    
    - name: Set up environment
      run: |
        echo "Running on ${{ matrix.platform }}"
        echo "OS: ${{ matrix.os }}"
    
    - name: Make script executable (Unix)
      if: matrix.os != 'windows-2025'
      run: chmod +x ./bench.sh
    
    - name: Run benchmark script (Unix)
      if: matrix.os != 'windows-2025'
      run: ./bench.sh
    
    - name: Run benchmark script (Windows)
      if: matrix.os == 'windows-2025'
      run: bash ./bench.sh
      shell: bash